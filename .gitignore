node_modules/
data/
.env
*.pem
.DS_Store
orders.json
.claude/

# Source/original assets not served by the app (kept on disk for future
# re-exports, not worth the repo weight — see assets/*.png served copies).
assets/logo-source-4k.png
assets/logo-alt.png

# Browser-test artifacts from the Playwright MCP
.playwright-mcp/
